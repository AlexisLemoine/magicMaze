#ifndef Court_chemin_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define Court_chemin_H

#include <queue>
#include <vector>
#include <fstream>

class Court_chemin{


public:
void add_edge(std::vector<int> adj[], int src, int dest);

};
#endif
